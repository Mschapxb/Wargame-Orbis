# Terrain à effets & cartes enrichies (1a) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter une couche de terrain (colline, bois, rivière, gué, pont, marais) qui influence déplacement, ligne de vue et combat, et enrichir Prairie, Forêt, Village et Défilé avec ce terrain.

**Architecture:** Un module pur `src/terrain.py` détient la table des terrains et toutes les règles (coûts, portée, modificateurs, ligne de vue). `Battlefield` porte une grille `terrain` parallèle à `grid` (jamais de nouveau code de cellule). Moteur (`unit.py`, `battle.py`, `battlefield.py`) et IA (`tactics.py`, `ai_commander.py`) appellent les mêmes fonctions. Une carte sans terrain (`bf.terrain is None`) garde exactement le comportement actuel. Le rendu vit dans un nouveau module `src/terrain_render.py`.

**Tech Stack:** Python 3.10+, Pygame 2.5+. Tests = scripts exécutables sans pytest (pytest n'est pas installé), dans le style de `src/test_edge_cases.py`.

**Spec:** `docs/superpowers/specs/2026-09-16-terrain-design.md`

## Global Constraints

- Aucune nouvelle dépendance (requirements: `pygame>=2.5.0` uniquement).
- `grid` n'est jamais étendu: codes `0..5` inchangés; le terrain vit dans `bf.terrain`.
- Carte sans `map_data['terrain']` → `bf.terrain is None` → toutes les fonctions de `terrain.py` rendent la valeur neutre (coût 1, bonus 0, mods 0, vue non bloquée).
- Noms de terrain (chaînes exactes): `"plaine"`, `"colline"`, `"bois"`, `"riviere"`, `"gue"`, `"pont"`, `"marais"`.
- Convention des seuils: un seuil plus **haut** = plus **difficile**. `combat_mods` renvoie des valeurs **à ajouter** au seuil de toucher et au seuil de sauvegarde.
- Déplacement: le budget d'un round consomme le coût de terrain par pas; au moins **1 pas** si un pas est possible et budget ≥ 1.
- Terrain des cartes enrichies **symétrique en miroir** (x ↔ width-1-x) sur les cases libres des deux côtés.
- Aucune unité ne commence la bataille sur une case de coût > 1.
- Équilibre: `bench_balance.py 60` — aucun affrontement ne varie de plus de **10 points** de taux de victoire vs la référence.
- Performance: durée totale de `bench_balance.py 60` ≤ **+20 %** vs référence.
- Ne pas toucher à l'estimation du rempart dans `tactics.expected_damage` (`extra_perf = -2`): défaut connu, traité au chantier 3.
- Messages de commit en français, terminés par `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- Toutes les commandes se lancent depuis `C:\Users\angel\Documents\Wargame-Orbis\src` sauf mention contraire.

## File Structure

| Fichier | Rôle |
|---|---|
| `src/terrain.py` (créer) | Table `TERRAINS` + règles pures: passabilité, coûts, charge, portée, modificateurs, ligne de vue |
| `src/terrain_render.py` (créer) | Motifs de terrain dans la surface statique + légende (touche `L`) |
| `src/test_terrain.py` (créer) | Tests exécutables du chantier |
| `src/test_determinism.py` (créer) | Vérifie qu'une graine rejoue la même bataille dans deux processus |
| `src/unit.py` (modifier) | `uid` stable; portée effective et `combat_mods` dans `perform_attacks` |
| `src/ai_commander.py`, `src/battle.py` (modifier) | Tris par `uid`; portée effective; charge et budget de mouvement |
| `src/battlefield.py` (modifier) | Grille `terrain`, `is_valid`, A*, ligne de vue, budget de pas |
| `src/tactics.py` (modifier) | `expected_damage` applique portée effective et `combat_mods` |
| `src/maps.py` (modifier) | Aides miroir/disque/chemin; terrain des 4 cartes; décor aligné |
| `src/renderer.py` (modifier) | Appel de `draw_terrain`, touche `L`, ligne d'aide |
| `README.md` (modifier) | Section Terrain, cartes, touche `L`, commandes de test |
| `docs/superpowers/baselines/` (créer) | Références d'équilibre avant/après |

---

### Task 1: Batailles reproductibles (uid stable)

Les tris par `id()` rendent une graine non reproductible (mesuré: Prairie graine 3 donne deux vainqueurs différents sur deux lancements). Sans ce correctif, aucune comparaison avant/après n'est fiable.

**Files:**
- Create: `src/test_determinism.py`
- Modify: `src/unit.py:1-12` (compteur + `self.uid`)
- Modify: `src/ai_commander.py:355, 737, 859, 941, 997, 1570`
- Modify: `src/battle.py:1484`

**Interfaces:**
- Produces: `Unit.uid: int` — unique par création, conservé par `copy.deepcopy`.

- [ ] **Step 1: Write the failing test**

Create `src/test_determinism.py`:

```python
"""Une graine doit rejouer EXACTEMENT la même bataille, d'un processus à
l'autre. Sans cela, impossible de mesurer l'effet d'un changement."""
import os, sys, random, subprocess

HERE = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, HERE)


def run_once():
    import unit_library as ul
    from battle import Battle
    out = []
    for m in ("Prairie", "Siège", "Forêt"):
        for seed in range(4):
            random.seed(1000 + seed)
            a1 = ul.build_army("Armée Skaldienne", [("Infanterie régulière", 8), ("Arbaletrier régulier", 4)])
            a2 = ul.build_army("Armée Skaldienne", [("Infanterie régulière", 5), ("Arbaletrier régulier", 4)])
            b = Battle(a1, a2, 40, 30, 8, map_name=m)
            while not b.is_battle_over() and b.round <= 90:
                b.simulate_round()
            hp = sum(u.hp for u in b.army1 + b.army2 if u.is_alive)
            out.append(f"{m}|{seed}|{b.is_battle_over()}|{b.round}|{hp}")
    return "\n".join(out)


if __name__ == "__main__":
    if len(sys.argv) > 1 and sys.argv[1] == "--child":
        sys.stdout.buffer.write(run_once().encode("utf-8"))
        sys.exit(0)
    runs = [subprocess.run([sys.executable, __file__, "--child"], capture_output=True).stdout
            for _ in range(3)]
    if runs[0] and runs[0] == runs[1] == runs[2]:
        print("OK   3 exécutions identiques")
        sys.exit(0)
    print("ECHEC: résultats différents d'une exécution à l'autre")
    for i, r in enumerate(runs):
        print(f"--- run {i}\n{r.decode('utf-8', 'replace')}")
    sys.exit(1)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python test_determinism.py`
Expected: `ECHEC: résultats différents d'une exécution à l'autre` (si par chance les 3 coïncident, relancer: l'échec a été mesuré sur Prairie graine 3).

- [ ] **Step 3: Add the stable uid**

In `src/unit.py`, after the imports (`from effects import FloatingText, FX_CLOCK`), add:

```python
import itertools

# Identifiant stable: sert de clé de tri déterministe (id() change d'une
# exécution à l'autre et rendait les graines non reproductibles).
_UID = itertools.count(1)
```

In `Unit.__init__`, right after `self.name = name`, add:

```python
        self.uid = next(_UID)
```

- [ ] **Step 4: Replace id() in sort keys**

Exact replacements (only sort/min keys; `lanes[id(u)]`, `used`, `_prey` dictionaries keyed by `id()` stay as they are):

`src/ai_commander.py:355`
```python
        scored.sort(key=lambda x: (-x[0], x[1].uid))
```
`src/ai_commander.py:737` — in the tuple, replace `id(e)` by `e.uid`:
```python
        lat = sorted(((e.position[0] - ec[0]) * px + (e.position[1] - ec[1]) * py, e.uid, e)
```
`src/ai_commander.py:859`
```python
            units.sort(key=lambda u: (u.position[1], u.uid))
```
`src/ai_commander.py:941` — replace `id(e)` by `e.uid` in the same way as line 737 (keep the rest of the line unchanged).

`src/ai_commander.py:997`
```python
        mobile.sort(key=lambda u: (u.position[1], u.uid))
```
`src/ai_commander.py:1570`
```python
            return min(in_r, key=lambda ed: (ed[0].hp / max(1, ed[0].max_hp), ed[0].uid))[0]
```
`src/battle.py:1484`
```python
            key=lambda kv: (-kv[0].vitesse, kv[0].uid))
```

Then check nothing is left: run `grep -n "key=.*id(\|, id(" *.py` from `src`. Expected: no line in `ai_commander.py`, `battle.py`, `battlefield.py`, `tactics.py`.

- [ ] **Step 5: Run test to verify it passes**

Run: `python test_determinism.py`
Expected: `OK   3 exécutions identiques`.
If it still fails: use superpowers:systematic-debugging. Other candidates are iteration over `set`s of units or `dict` keyed by `id()` whose iteration order feeds a decision; fix them the same way (sort by `uid`). Do not continue until it passes.

- [ ] **Step 6: Run the existing suites**

Run: `python test_edge_cases.py` → Expected last line `Tous les cas limites passent.`
Run: `python test_ai_headless.py` → Expected last line `Tous les tests terminés.` and no traceback.

- [ ] **Step 7: Commit**

```bash
git add src/unit.py src/ai_commander.py src/battle.py src/test_determinism.py
git commit -m "fix: batailles reproductibles — tri par uid stable au lieu de id()

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: Référence d'équilibre avant terrain

**Files:**
- Create: `docs/superpowers/baselines/bench-before-terrain.txt`

- [ ] **Step 1: Capture the baseline**

Run (from `src`): `python bench_balance.py 60 > ../docs/superpowers/baselines/bench-before-terrain.txt`
Expected: 6 lines `Melee vs Archers … Foret mixte` plus a timing line `(NN.Ns)`. Takes several minutes.

- [ ] **Step 2: Check reproducibility of the baseline**

Run: `python bench_balance.py 5` twice. Expected: identical output except the `(N.Ns)` timing line.

- [ ] **Step 3: Commit**

```bash
git add ../docs/superpowers/baselines/bench-before-terrain.txt
git commit -m "test: référence d'équilibre avant le terrain (60 graines)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: Module `terrain.py` (règles pures)

**Files:**
- Create: `src/terrain.py`
- Create: `src/test_terrain.py`

**Interfaces:**
- Produces (all in `terrain`):
  - constants `PLAIN, HILL, WOOD, RIVER, FORD, BRIDGE, MARSH` (str), `TERRAINS: dict[str, dict]`, `MOVE: dict[str, float|None]`, `ELEVATED: frozenset[str]`
  - `make_grid(width, height, fill=PLAIN) -> list[list[str]]`
  - `at(bf, x, y) -> str`
  - `is_passable(bf, x, y) -> bool`, `is_elevated(bf, x, y) -> bool`, `charge_ok(bf, x, y) -> bool`
  - `step_cost(bf, frm, to) -> float` (inf if impassable)
  - `move_cost(bf, frm, to) -> float` (Chebyshev distance × `step_cost`)
  - `path_cost(bf, start, path) -> float`
  - `steps_within(bf, start, path, budget) -> int`
  - `can_charge(bf, frm, to) -> bool`
  - `range_bonus(bf, shooter, target) -> int`
  - `effective_range(bf, shooter, target) -> int`
  - `weapon_reach(bf, arme, shooter, target) -> int`
  - `combat_mods(bf, attacker, target, ranged) -> dict` with keys `'toucher'`, `'save'`
  - `line_cells(x0, y0, x1, y1) -> list[tuple]` (intermediate Bresenham cells, endpoints excluded)
  - `blocks_line(bf, x0, y0, x1, y1) -> bool`

`bf` only needs attributes `terrain` (grid or `None`), `width`, `height`. Units only need `position` and `_max_range`.

- [ ] **Step 1: Write the failing tests**

Create `src/test_terrain.py`:

```python
"""Tests du terrain à effets (chantier 1a). Script exécutable:
    python test_terrain.py            # tout
    python test_terrain.py charge     # seulement les tests dont le nom contient 'charge'
"""
import os, sys, random, traceback

HERE = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, HERE)

import terrain as tr

TESTS = []


def test(fn):
    TESTS.append(fn)
    return fn


class FakeBF:
    def __init__(self, w=12, h=8, cells=None, with_terrain=True):
        self.width, self.height = w, h
        self.terrain = tr.make_grid(w, h) if with_terrain else None
        for (x, y), name in (cells or {}).items():
            self.terrain[x][y] = name


class U:
    def __init__(self, pos, max_range=1):
        self.position = pos
        self._max_range = max_range


# ── terrain.py: règles pures ──

@test
def test_no_terrain_is_neutral():
    bf = FakeBF(with_terrain=False)
    a, b = U((1, 1), 8), U((6, 1))
    assert tr.at(bf, 3, 3) == tr.PLAIN
    assert tr.is_passable(bf, 3, 3)
    assert tr.step_cost(bf, (1, 1), (2, 2)) == 1.0
    assert tr.range_bonus(bf, a, b) == 0
    assert tr.combat_mods(bf, a, b, True) == {'toucher': 0, 'save': 0}
    assert tr.blocks_line(bf, 0, 0, 10, 0) is False
    assert tr.steps_within(bf, (0, 0), [(1, 0), (2, 0), (3, 0)], 2) == 2


@test
def test_step_costs():
    bf = FakeBF(cells={(2, 0): tr.WOOD, (3, 0): tr.MARSH, (4, 0): tr.RIVER,
                       (5, 0): tr.HILL, (6, 0): tr.HILL, (7, 0): tr.FORD, (8, 0): tr.BRIDGE})
    assert tr.step_cost(bf, (1, 0), (2, 0)) == 2.0
    assert tr.step_cost(bf, (2, 0), (3, 0)) == 3.0
    assert tr.step_cost(bf, (3, 0), (4, 0)) == float('inf')
    assert not tr.is_passable(bf, 4, 0)
    assert tr.step_cost(bf, (4, 1), (5, 0)) == 1.5   # on monte
    assert tr.step_cost(bf, (5, 0), (6, 0)) == 1.0   # déjà en haut
    assert tr.step_cost(bf, (6, 0), (7, 0)) == 2.0
    assert tr.step_cost(bf, (7, 0), (8, 0)) == 1.0
    assert tr.move_cost(bf, (0, 2), (2, 0)) == 2.0 * 2.0  # 2 pas vers un bois


@test
def test_steps_within_budget():
    bf = FakeBF(cells={(1, 0): tr.WOOD, (2, 0): tr.WOOD, (3, 0): tr.MARSH})
    path = [(1, 0), (2, 0), (3, 0)]
    assert tr.steps_within(bf, (0, 0), path, 4) == 2        # 2 + 2
    assert tr.steps_within(bf, (0, 0), path, 3) == 1
    assert tr.steps_within(bf, (2, 0), [(3, 0)], 2) == 1    # au moins 1 pas
    assert tr.steps_within(bf, (0, 0), path, 0) == 0
    assert tr.steps_within(bf, (0, 0), [], 5) == 0
    assert tr.path_cost(bf, (0, 0), path) == 7.0


@test
def test_charge_rules():
    bf = FakeBF(cells={(2, 2): tr.WOOD, (3, 3): tr.FORD, (4, 4): tr.MARSH, (5, 5): tr.HILL})
    assert tr.can_charge(bf, (0, 0), (1, 1))
    assert not tr.can_charge(bf, (2, 2), (1, 1))   # départ en bois
    assert not tr.can_charge(bf, (0, 0), (2, 2))   # impact en bois
    assert not tr.can_charge(bf, (0, 0), (3, 3))
    assert not tr.can_charge(bf, (0, 0), (4, 4))
    assert tr.can_charge(bf, (0, 0), (5, 5))


@test
def test_hill_range_bonus():
    bf = FakeBF(cells={(1, 1): tr.HILL, (6, 1): tr.HILL})
    archer_up = U((1, 1), 8)
    assert tr.range_bonus(bf, archer_up, U((5, 1))) == 1
    assert tr.effective_range(bf, archer_up, U((5, 1))) == 9
    assert tr.range_bonus(bf, archer_up, U((6, 1))) == 0     # cible aussi en hauteur
    assert tr.range_bonus(bf, U((2, 2), 8), U((5, 1))) == 0  # tireur en plaine
    assert tr.range_bonus(bf, U((1, 1), 1), U((2, 1))) == 0  # mêlée: pas de bonus

    class A:
        porte = 8
    class M:
        porte = 1
    assert tr.weapon_reach(bf, A, archer_up, U((5, 1))) == 9
    assert tr.weapon_reach(bf, M, archer_up, U((5, 1))) == 1


@test
def test_combat_mods():
    bf = FakeBF(cells={(5, 1): tr.WOOD, (5, 2): tr.HILL, (5, 3): tr.FORD,
                       (5, 4): tr.MARSH, (4, 2): tr.HILL})
    shooter = U((0, 0), 8)
    assert tr.combat_mods(bf, shooter, U((5, 1)), True) == {'toucher': 1, 'save': 0}
    assert tr.combat_mods(bf, U((4, 1)), U((5, 1)), False) == {'toucher': 0, 'save': 0}
    assert tr.combat_mods(bf, U((6, 2)), U((5, 2)), False) == {'toucher': 1, 'save': 0}
    assert tr.combat_mods(bf, U((4, 2)), U((5, 2)), False) == {'toucher': 0, 'save': 0}
    assert tr.combat_mods(bf, shooter, U((5, 2)), True) == {'toucher': 0, 'save': 0}
    assert tr.combat_mods(bf, U((4, 3)), U((5, 3)), False) == {'toucher': 0, 'save': 1}
    assert tr.combat_mods(bf, shooter, U((5, 4)), True) == {'toucher': 0, 'save': 1}


@test
def test_line_of_sight():
    bf = FakeBF(w=14, cells={(3, 0): tr.WOOD, (4, 0): tr.WOOD, (5, 0): tr.WOOD,
                             (3, 2): tr.WOOD, (4, 2): tr.WOOD,
                             (0, 0): tr.PLAIN})
    assert tr.line_cells(0, 0, 3, 0) == [(1, 0), (2, 0)]
    assert tr.blocks_line(bf, 0, 0, 8, 0) is True     # 3 bois intermédiaires
    assert tr.blocks_line(bf, 0, 2, 8, 2) is False    # 2 bois seulement
    bf.terrain[0][0] = tr.HILL
    assert tr.blocks_line(bf, 0, 0, 8, 0) is False    # tireur en hauteur
    bf2 = FakeBF(w=14, cells={(4, 4): tr.HILL})
    assert tr.blocks_line(bf2, 0, 4, 8, 4) is True    # colline entre les deux
    bf2.terrain[8][4] = tr.HILL
    assert tr.blocks_line(bf2, 0, 4, 8, 4) is False   # cible sur la colline


# ── Runner (ajouter les nouveaux tests AU-DESSUS de cette ligne) ──

if __name__ == "__main__":
    only = sys.argv[1] if len(sys.argv) > 1 else None
    fails = []
    for fn in TESTS:
        if only and only not in fn.__name__:
            continue
        try:
            random.seed(7)
            fn()
            print(f"  OK    {fn.__name__}")
        except Exception:
            fails.append(fn.__name__)
            print(f"  ECHEC {fn.__name__}")
            traceback.print_exc()
    print()
    print(f"{len(fails)} ECHEC(S)" if fails else "Tous les tests terrain passent.")
    sys.exit(1 if fails else 0)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `python test_terrain.py`
Expected: `ModuleNotFoundError: No module named 'terrain'`.

- [ ] **Step 3: Write `src/terrain.py`**

```python
"""Terrain à effets — la SEULE source de vérité des règles de terrain.

Le moteur (déplacement, combat) et l'IA (estimation des dégâts, ciblage)
appellent ces fonctions: ils voient donc exactement les mêmes chiffres.

La grille `bf.terrain` est parallèle à `bf.grid` et ne la remplace pas:
`grid` dit ce qui est physiquement bâti (obstacle, mur, porte…), `terrain`
dit sur quoi l'on marche. Une carte sans terrain (`bf.terrain is None`) se
joue exactement comme avant: toutes les fonctions rendent la valeur neutre.

Convention des seuils (d6): un seuil plus HAUT est plus DIFFICILE.
`combat_mods` renvoie des valeurs à AJOUTER aux seuils.
"""

PLAIN, HILL, WOOD, RIVER, FORD, BRIDGE, MARSH = (
    "plaine", "colline", "bois", "riviere", "gue", "pont", "marais")

TERRAINS = {
    PLAIN:  dict(move=1.0,  passable=True,  charge=True,  save_mod=0,  cover=0, blocks_los=0, elevated=False),
    HILL:   dict(move=1.0,  passable=True,  charge=True,  save_mod=0,  cover=0, blocks_los=0, elevated=True),
    WOOD:   dict(move=2.0,  passable=True,  charge=False, save_mod=0,  cover=1, blocks_los=1, elevated=False),
    RIVER:  dict(move=None, passable=False, charge=False, save_mod=0,  cover=0, blocks_los=0, elevated=False),
    FORD:   dict(move=2.0,  passable=True,  charge=False, save_mod=-1, cover=0, blocks_los=0, elevated=False),
    BRIDGE: dict(move=1.0,  passable=True,  charge=True,  save_mod=0,  cover=0, blocks_los=0, elevated=False),
    MARSH:  dict(move=3.0,  passable=True,  charge=False, save_mod=-1, cover=0, blocks_los=0, elevated=False),
}

# Accès rapides pour les boucles chaudes (A*)
MOVE = {name: t['move'] for name, t in TERRAINS.items()}
ELEVATED = frozenset(name for name, t in TERRAINS.items() if t['elevated'])

UPHILL_FACTOR = 1.5     # monter sur une colline coûte plus cher
WOODS_BLOCKING = 3      # nombre de cases de bois qui masquent un tir
_NO_MODS = {'toucher': 0, 'save': 0}
_INF = float('inf')


def make_grid(width, height, fill=PLAIN):
    return [[fill] * height for _ in range(width)]


def at(bf, x, y):
    terr = getattr(bf, 'terrain', None)
    if terr is None:
        return PLAIN
    return terr[x][y]


def is_passable(bf, x, y):
    return TERRAINS[at(bf, x, y)]['passable']


def is_elevated(bf, x, y):
    return at(bf, x, y) in ELEVATED


def charge_ok(bf, x, y):
    return TERRAINS[at(bf, x, y)]['charge']


def step_cost(bf, frm, to):
    """Multiplicateur de coût d'un pas de `frm` vers la case voisine `to`."""
    if getattr(bf, 'terrain', None) is None:
        return 1.0
    dest = bf.terrain[to[0]][to[1]]
    m = MOVE[dest]
    if m is None:
        return _INF
    if dest in ELEVATED and bf.terrain[frm[0]][frm[1]] not in ELEVATED:
        m *= UPHILL_FACTOR
    return m


def move_cost(bf, frm, to):
    """Coût d'un déplacement direct (en pas de Chebyshev) vers `to`."""
    steps = max(abs(to[0] - frm[0]), abs(to[1] - frm[1]))
    return steps * step_cost(bf, frm, to)


def path_cost(bf, start, path):
    total, prev = 0.0, start
    for cell in path:
        total += step_cost(bf, prev, cell)
        prev = cell
    return total


def steps_within(bf, start, path, budget):
    """Nombre de pas du chemin que le budget permet. Au moins 1 pas si le
    budget vaut au moins 1 (sinon une unité lente resterait figée dans un
    marais)."""
    if not path or budget < 1:
        return 0
    if getattr(bf, 'terrain', None) is None:
        return min(int(budget), len(path))
    spent, prev, n = 0.0, start, 0
    for cell in path:
        c = step_cost(bf, prev, cell)
        if c == _INF:
            break
        if n >= 1 and spent + c > budget:
            break
        spent += c
        n += 1
        prev = cell
    return n


def can_charge(bf, frm, to):
    return charge_ok(bf, *frm) and charge_ok(bf, *to)


def range_bonus(bf, shooter, target):
    """+1 de portée pour un tireur en hauteur visant une cible en contrebas."""
    if getattr(bf, 'terrain', None) is None or shooter._max_range < 4:
        return 0
    if is_elevated(bf, *shooter.position) and not is_elevated(bf, *target.position):
        return 1
    return 0


def effective_range(bf, shooter, target):
    return shooter._max_range + range_bonus(bf, shooter, target)


def weapon_reach(bf, arme, shooter, target):
    if arme.porte >= 4:
        return arme.porte + range_bonus(bf, shooter, target)
    return arme.porte


def combat_mods(bf, attacker, target, ranged):
    if getattr(bf, 'terrain', None) is None:
        return dict(_NO_MODS)
    t = TERRAINS[at(bf, *target.position)]
    toucher = 0
    if ranged:
        toucher += t['cover']
    elif t['elevated'] and not is_elevated(bf, *attacker.position):
        toucher += 1
    return {'toucher': toucher, 'save': -t['save_mod']}


def line_cells(x0, y0, x1, y1):
    """Cases intermédiaires d'un tracé de Bresenham (extrémités exclues).
    Même tracé que Battlefield._los_clear."""
    cells = []
    dx, dy = abs(x1 - x0), abs(y1 - y0)
    sx = 1 if x1 > x0 else -1
    sy = 1 if y1 > y0 else -1
    err = dx - dy
    x, y = x0, y0
    while True:
        if (x != x0 or y != y0) and (x != x1 or y != y1):
            cells.append((x, y))
        if x == x1 and y == y1:
            return cells
        e2 = 2 * err
        if e2 > -dy:
            err -= dy
            x += sx
        if e2 < dx:
            err += dx
            y += sy


def blocks_line(bf, x0, y0, x1, y1):
    """Vrai si le terrain masque la cible. Un tireur en hauteur voit tout;
    sinon une colline intermédiaire (cible hors colline) ou 3 cases de bois
    bloquent."""
    terr = getattr(bf, 'terrain', None)
    if terr is None or terr[x0][y0] in ELEVATED:
        return False
    target_up = terr[x1][y1] in ELEVATED
    woods = 0
    for x, y in line_cells(x0, y0, x1, y1):
        name = terr[x][y]
        if name in ELEVATED and not target_up:
            return True
        if TERRAINS[name]['blocks_los']:
            woods += 1
            if woods >= WOODS_BLOCKING:
                return True
    return False
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python test_terrain.py`
Expected: 7 `OK` lines, `Tous les tests terrain passent.`

- [ ] **Step 5: Commit**

```bash
git add terrain.py test_terrain.py
git commit -m "feat(terrain): module de règles de terrain (coûts, charge, portée, vue, modificateurs)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: Battlefield — terrain, A*, ligne de vue, budget de pas

**Files:**
- Modify: `src/battlefield.py` (imports; `__init__`; `is_valid`; `has_line_of_fire`; `a_star_path` neighbor loop; `find_best_attack_position` inline validity; 5 `steps = min(...)` sites in `compute_move`)
- Test: `src/test_terrain.py`

**Interfaces:**
- Consumes: `terrain.MOVE`, `terrain.ELEVATED`, `terrain.UPHILL_FACTOR`, `terrain.blocks_line`, `terrain.steps_within`, `terrain.effective_range`
- Produces: `Battlefield.terrain` (grid or `None`); `is_valid` rejects impassable terrain; `a_star_path` weights steps by terrain; `has_line_of_fire` honours terrain.

- [ ] **Step 1: Write the failing tests**

Add above the runner marker in `src/test_terrain.py`:

```python
# ── Battlefield ──

from battlefield import Battlefield


class FakeBattle:
    def get_allies(self, unit):
        return []


def real_bf(w=16, h=9, cells=None, grid=None):
    terr = tr.make_grid(w, h)
    for (x, y), name in (cells or {}).items():
        terr[x][y] = name
    g = grid or [[0] * h for _ in range(w)]
    return Battlefield(w, h, 0, "Prairie", g, {'terrain': terr})


@test
def test_bf_terrain_not_siege():
    bf = real_bf()
    assert bf.terrain is not None
    assert not bf.siege_data, "le terrain ne doit pas faire croire à un siège"
    plain = Battlefield(10, 6, 0, "Prairie", [[0] * 6 for _ in range(10)], {})
    assert plain.terrain is None


@test
def test_bf_river_invalid():
    bf = real_bf(cells={(5, 3): tr.RIVER})
    assert not bf.is_valid(5, 3)
    assert bf.is_valid(5, 4)


@test
def test_astar_avoids_marsh_and_river():
    w, h = 16, 9
    marsh = {(x, y): tr.MARSH for x in range(6, 10) for y in range(0, 6)}
    bf = real_bf(w, h, cells=marsh)
    u = U((2, 3))
    path = bf.a_star_path((2, 3), (13, 3), u, FakeBattle())
    assert path and path[-1] == (13, 3)
    assert not any(bf.terrain[x][y] == tr.MARSH for x, y in path), path
    river = {(8, y): tr.RIVER for y in range(h)}
    river[(8, 7)] = tr.FORD
    bf2 = real_bf(w, h, cells=river)
    path2 = bf2.a_star_path((2, 3), (13, 3), u, FakeBattle(), max_nodes=5000)
    assert (8, 7) in path2, path2
    assert not any(bf2.terrain[x][y] == tr.RIVER for x, y in path2)


@test
def test_los_uses_terrain_without_walls():
    bf = real_bf(cells={(3, 0): tr.WOOD, (4, 0): tr.WOOD, (5, 0): tr.WOOD})
    assert bf.has_line_of_fire(U((0, 0), 8), U((8, 0))) is False
    assert bf.has_line_of_fire(U((0, 1), 8), U((8, 1))) is True
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `python test_terrain.py bf_`
Expected: ECHEC on `test_bf_terrain_not_siege` (`AttributeError: 'Battlefield' object has no attribute 'terrain'`) and on the others.
Run: `python test_terrain.py astar` → ECHEC.

- [ ] **Step 3: Wire terrain into Battlefield**

In `src/battlefield.py`, imports become:

```python
import random
import heapq

import terrain as tr
```

In `__init__`, right after `self.deploy_gap = _raw.pop('deploy_gap', None)`, add:

```python
        # Terrain à effets (colline, bois, rivière…): grille parallèle à
        # `grid`. Extrait AVANT siege_data pour la même raison que le décor.
        self.terrain = _raw.pop('terrain', None)
```

In `is_valid`, replace the first lines:

```python
    def is_valid(self, x, y):
        if not (0 <= x < self.width and 0 <= y < self.height):
            return False
        if self.terrain is not None and tr.MOVE[self.terrain[x][y]] is None:
            return False  # Rivière: infranchissable
        cell = self.grid[x][y]
```

(the rest of the method is unchanged).

- [ ] **Step 4: Terrain-aware line of fire**

Replace the body of `has_line_of_fire` with:

```python
        sx, sy = shooter.position
        tx, ty = target.position
        if not self.walls and not self.gate_hp:
            if self.terrain is None:
                return True  # Ni fortifications ni terrain sur cette carte
            return not tr.blocks_line(self, sx, sy, tx, ty)
        if self.is_rampart(sx, sy) or self.is_rampart(tx, ty):
            return True
        if not self._los_clear(sx, sy, tx, ty):
            return False
        return self.terrain is None or not tr.blocks_line(self, sx, sy, tx, ty)
```

Keep the existing docstring and append one line: `Le terrain (bois, collines) peut aussi masquer la cible, cf. terrain.blocks_line.`

- [ ] **Step 5: Terrain cost in A***

In `a_star_path`, in the local caches block (after `gates_open = self.gates_open`), add:

```python
        terr = self.terrain
        _move = tr.MOVE
        _elev = tr.ELEVATED
        _uphill = tr.UPHILL_FACTOR
```

In the neighbor loop, replace:

```python
                base_cost = _DIAG_COST if (dx and dy) else 1.0
```

with:

```python
                base_cost = _DIAG_COST if (dx and dy) else 1.0
                if terr is not None:
                    tn = terr[nx][ny]
                    mc = _move[tn]
                    if mc is None:
                        continue  # rivière
                    if tn in _elev and terr[cx][cy] not in _elev:
                        mc *= _uphill
                    base_cost *= mc
```

(Heuristic unchanged: every step costs ≥ its Chebyshev length, so it stays admissible.)

In `find_best_attack_position`, after `gate_hp_dict = self.gate_hp` add `terr = self.terrain`, and right after the inlined `if cell == 3 and …: continue` add:

```python
                if terr is not None and tr.MOVE[terr[px][py]] is None:
                    continue
```

- [ ] **Step 6: Movement budget in compute_move**

Replace each of these 5 lines (at `battlefield.py` ~445, ~609, ~664, ~688, ~702):

```python
                steps = min(flee_speed, len(path))
```
→
```python
                steps = tr.steps_within(self, unit.position, path, flee_speed)
```

```python
                steps = min(unit.vitesse, len(path))
```
→
```python
                steps = tr.steps_within(self, unit.position, path, unit.vitesse)
```

```python
                        steps = min(unit.vitesse, len(gpath))
```
→
```python
                        steps = tr.steps_within(self, unit.position, gpath, unit.vitesse)
```

(the same replacement for the second `gpath` site, with its own indentation). Confirm with `grep -n "min(unit.vitesse\|min(flee_speed" battlefield.py` → no output.

- [ ] **Step 7: Run tests**

Run: `python test_terrain.py` → Expected `Tous les tests terrain passent.`
Run: `python test_edge_cases.py` → `Tous les cas limites passent.`
Run: `python test_determinism.py` → `OK   3 exécutions identiques`.

- [ ] **Step 8: Commit**

```bash
git add battlefield.py test_terrain.py
git commit -m "feat(terrain): coûts de déplacement, rivière et ligne de vue dans Battlefield

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: Combat, charge, portée effective (moteur + IA)

**Files:**
- Modify: `src/unit.py` (`import terrain as tr`; `perform_attacks`)
- Modify: `src/tactics.py` (`import terrain`; `expected_damage`)
- Modify: `src/battle.py` (`import terrain as tr`; `_charge_phase`; movement application; `_reaction_fire`; `_choose_attack_target`)
- Modify: `src/battlefield.py` (`compute_move` range checks)
- Modify: `src/ai_commander.py` (`import terrain as tr`; `assess`; `_pick_focus_target`; `select_tactical_target`)
- Test: `src/test_terrain.py`

**Interfaces:**
- Consumes: `tr.range_bonus`, `tr.weapon_reach`, `tr.effective_range`, `tr.combat_mods`, `tr.charge_ok`, `tr.path_cost`, `tr.move_cost`

- [ ] **Step 1: Write the failing tests**

Add above the runner marker in `src/test_terrain.py`:

```python
# ── Combat ──

from models import Arme
from unit import Unit
import tactics


def archer(pos, save=7):
    u = Unit("Archer", pv=100, vitesse=4, morale=3, sauvegarde=save, color=(1, 1, 1),
             armes=[Arme("Arc", nb_attaque=1, toucher=3, blesser=1, perforation=0,
                         degats="1", porte=8)])
    u.position = pos
    return u


def target(pos, save=7):
    u = Unit("Cible", pv=100000, vitesse=4, morale=5, sauvegarde=save, color=(2, 2, 2),
             armes=[Arme("Epee", 1, 4, 4, 0, "1", porte=1)])
    u.position = pos
    return u


def mean_damage(bf, a, t, n=3000):
    total = 0
    for _ in range(n):
        before = t.hp
        a.perform_attacks(t, bf)
        total += before - t.hp
        t.hp = t.max_hp
        t.is_alive = True
    return total / n


@test
def test_wood_cover_engine_matches_estimate():
    bf = real_bf(cells={(6, 1): tr.WOOD})
    a, t = archer((0, 1)), target((6, 1))
    bf.place_unit(a); bf.place_unit(t)
    est = tactics.expected_damage(a, t, 6, bf)
    assert abs(est - 3 / 6) < 1e-9, est          # toucher 3 → 4+ en bois: 3/6
    got = mean_damage(bf, a, t)
    assert abs(got - est) / est < 0.10, (got, est)


@test
def test_marsh_worsens_save_engine_matches_estimate():
    bf = real_bf(cells={(1, 1): tr.MARSH})
    a, t = archer((0, 1)), target((1, 1), save=4)
    a.armes[0].porte = 1
    a._max_range = 1
    est = tactics.expected_damage(a, t, 1, bf)
    # toucher 3+ (4/6) × blesser 1+ (1) × échec de sauvegarde 5+ (4/6)
    assert abs(est - (4 / 6) * (4 / 6)) < 1e-9, est
    got = mean_damage(bf, a, t)
    assert abs(got - est) / est < 0.10, (got, est)


@test
def test_hill_gives_range():
    bf = real_bf(w=16, cells={(0, 1): tr.HILL})
    a, t = archer((0, 1)), target((9, 1))
    assert tactics.expected_damage(a, t, 9, bf) > 0
    assert mean_damage(bf, a, t, 400) > 0
    a2 = archer((0, 2))
    assert mean_damage(bf, a2, target((9, 2)), 50) == 0


def charge_setup(cells):
    import unit_library as ul
    from battle import Battle
    random.seed(3)
    rider = Unit("Cavalier", pv=10, vitesse=6, morale=5, sauvegarde=4, color=(9, 9, 9),
                 armes=[Arme("Lance", 2, 3, 3, 1, "2", porte=1)])
    rider.charge_montee = True
    foe = Unit("Fantassin", pv=10, vitesse=0, morale=5, sauvegarde=5, color=(8, 8, 8),
               armes=[Arme("Epee", 1, 4, 4, 0, "1", porte=1)])
    b = Battle([rider], [foe], 30, 12, 0, map_name="Prairie")
    bf = b.battlefield
    bf.grid = [[0] * bf.height for _ in range(bf.width)]
    bf.terrain = tr.make_grid(bf.width, bf.height)
    for (x, y), name in cells.items():
        bf.terrain[x][y] = name
    r, f = b.army1[0], b.army2[0]
    bf.move_unit(r, (10, 5))
    bf.move_unit(f, (15, 5))
    r._cells_moved = 0
    return b, r, f


@test
def test_charge_blocked_by_wood_impact():
    around = {(15 + dx, 5 + dy): tr.WOOD for dx in (-1, 0, 1) for dy in (-1, 0, 1)
              if (dx, dy) != (0, 0)}
    b, r, f = charge_setup(around)
    b._charge_phase([r, f])
    assert r.position == (10, 5) and not getattr(r, '_charged_this_round', False)


@test
def test_charge_allowed_on_plain():
    b, r, f = charge_setup({})
    b._charge_phase([r, f])
    assert r.position != (10, 5)


@test
def test_charge_blocked_from_marsh():
    b, r, f = charge_setup({(10, 5): tr.MARSH})
    b._charge_phase([r, f])
    assert r.position == (10, 5)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `python test_terrain.py wood_cover` → ECHEC (`est` ≈ 0.667, cover not applied).
Run: `python test_terrain.py charge` → ECHEC on the two "blocked" tests.
Run: `python test_terrain.py hill_gives` → ECHEC.
(`_charged_this_round` is only created by `battle.py` during a round, hence the `getattr` in the test.)

- [ ] **Step 3: Engine — perform_attacks**

In `src/unit.py`, add `import terrain as tr` after `from effects import FloatingText, FX_CLOCK`.

In `perform_attacks`, replace:

```python
        if dist > self._max_range or self.fleeing:
```

with:

```python
        range_bonus = tr.range_bonus(battlefield, self, target)
        if dist > self._max_range + range_bonus or self.fleeing:
```

In the weapon loop, replace:

```python
        for arme in armes:
            if dist > arme.porte:
                continue
```

with:

```python
        for arme in armes:
            if dist > tr.weapon_reach(battlefield, arme, self, target):
                continue
```

Right after `is_ranged_weapon = arme.porte >= 4`, add:

```python
            tmods = tr.combat_mods(battlefield, self, target, is_ranged_weapon)
```

Replace the toucher line:

```python
                toucher_final = (arme.toucher + (1 if self.afraid else 0)
                                 + anti_toucher + charge_toucher + wall_toucher_bonus
                                 + flank_toucher + snap_toucher)
```

with:

```python
                toucher_final = (arme.toucher + (1 if self.afraid else 0)
                                 + anti_toucher + charge_toucher + wall_toucher_bonus
                                 + flank_toucher + snap_toucher + tmods['toucher'])
```

Replace the save line:

```python
                save_modifie = min(7, target.sauvegarde - perf_final - wall_save_bonus)
```

with:

```python
                save_modifie = min(7, target.sauvegarde - perf_final - wall_save_bonus
                                   + tmods['save'])
```

- [ ] **Step 4: Estimate — tactics.expected_damage**

In `src/tactics.py`, after `import math` add `import terrain`.

Replace the weapons loop of `expected_damage`:

```python
    for arme in attacker.armes:
        if dist is not None and dist > arme.porte:
            continue
        total += weapon_expected_damage(arme, attacker, target, extra_perf,
                                        anti_mod, anti_mod)
```

with:

```python
    for arme in attacker.armes:
        ranged = arme.porte >= 4
        if dist is not None:
            reach = (terrain.weapon_reach(battlefield, arme, attacker, target)
                     if battlefield is not None else arme.porte)
            if dist > reach:
                continue
        tm = (terrain.combat_mods(battlefield, attacker, target, ranged)
              if battlefield is not None else {'toucher': 0, 'save': 0})
        # save +1 (seuil) ⇔ perforation supplémentaire -1 dans cette formule
        total += weapon_expected_damage(arme, attacker, target, extra_perf - tm['save'],
                                        anti_mod + tm['toucher'], anti_mod)
```

(The `extra_perf = -2` rampart line above stays untouched — see Global Constraints.)

- [ ] **Step 5: Charge phase and movement budget in battle.py**

In `src/battle.py`, add `import terrain as tr` next to the other top-level imports.

In `_charge_phase`, right after:

```python
            if not unit.is_alive or unit.fleeing:
                continue
```

add:

```python
            # Pas d'élan depuis un bois, un gué ou un marais
            if not tr.charge_ok(self.battlefield, *unit.position):
                continue
```

In the charge-cell search, replace:

```python
                    if self.battlefield._can_move_to(unit, (nx, ny), set()):
```

with:

```python
                    if (self.battlefield._can_move_to(unit, (nx, ny), set())
                            and tr.charge_ok(self.battlefield, nx, ny)):
```

Replace:

```python
            if not path or len(path) > budget:
                continue

            start_pos = unit.position
            self.battlefield.move_unit(unit, charge_pos)
            unit._cells_moved += len(path)
```

with:

```python
            if not path:
                continue
            cost = tr.path_cost(self.battlefield, unit.position, path)
            if cost > budget:
                continue

            start_pos = unit.position
            self.battlefield.move_unit(unit, charge_pos)
            unit._cells_moved += cost
```

In the movement application of `simulate_round`, replace:

```python
            unit._cells_moved += max(abs(new_pos[0] - old_pos[0]),
                                     abs(new_pos[1] - old_pos[1]))
```

with:

```python
            unit._cells_moved += tr.move_cost(self.battlefield, old_pos, new_pos)
```

In `_reaction_fire`, delete the line `mr = shooter._max_range` and, inside the enemy loop, right after `old_pos, new_pos = info`, add:

```python
                mr = tr.effective_range(bf, shooter, e)
```

In `_choose_attack_target`, replace in `can_hit`:

```python
            if abs(ux - e.position[0]) + abs(uy - e.position[1]) > mr:
```

with:

```python
            if abs(ux - e.position[0]) + abs(uy - e.position[1]) > tr.effective_range(bf, unit, e):
```

- [ ] **Step 6: Effective range in compute_move**

In `src/battlefield.py` `compute_move`:

Artillery block — replace:

```python
                         if abs(ux_a - e.position[0]) + abs(uy_a - e.position[1]) <= mr
```

with:

```python
                         if abs(ux_a - e.position[0]) + abs(uy_a - e.position[1]) <= tr.effective_range(self, unit, e)
```

Rampart shooters block — replace:

```python
                            if abs(ux - e.position[0]) + abs(uy - e.position[1]) <= mr
```

with:

```python
                            if abs(ux - e.position[0]) + abs(uy - e.position[1]) <= tr.effective_range(self, unit, e)
```

Sticky combat — replace:

```python
        if (closest_dist <= unit._max_range and not unit.fleeing
                and not _is_kiting and not _is_repositioning):
            # En mêlée: ne pas bouger, combattre le plus proche (ou le plus blessé à portée)
            in_range = [e for e in enemies if abs(ux - e.position[0]) + abs(uy - e.position[1]) <= unit._max_range]
```

with:

```python
        if (closest_enemy is not None
                and closest_dist <= tr.effective_range(self, unit, closest_enemy)
                and not unit.fleeing and not _is_kiting and not _is_repositioning):
            # En mêlée: ne pas bouger, combattre le plus proche (ou le plus blessé à portée)
            in_range = [e for e in enemies
                        if abs(ux - e.position[0]) + abs(uy - e.position[1])
                        <= tr.effective_range(self, unit, e)]
```

Support/guard post — replace:

```python
                            if abs(ux - e.position[0]) + abs(uy - e.position[1]) <= mr
```

(the one inside `in_r = [...]` after `mr = unit._max_range`) with:

```python
                            if abs(ux - e.position[0]) + abs(uy - e.position[1]) <= tr.effective_range(self, unit, e)
```

Target in range — replace:

```python
        if current_dist <= unit._max_range:
```

with:

```python
        if current_dist <= tr.effective_range(self, unit, target):
```

- [ ] **Step 7: Effective range in the AI**

In `src/ai_commander.py`, add `import terrain as tr` after `import tactics`.

In `assess`, replace:

```python
            rng_a = a._max_range
            for e in theirs:
                d = abs(a.position[0] - e.position[0]) + abs(a.position[1] - e.position[1])
                if d <= rng_a and bf.has_line_of_fire(a, e):
```

with:

```python
            for e in theirs:
                d = abs(a.position[0] - e.position[0]) + abs(a.position[1] - e.position[1])
                if d <= tr.effective_range(bf, a, e) and bf.has_line_of_fire(a, e):
```

and in `support_fire`, replace `<= u._max_range and bf.has_line_of_fire(u, e) for e in theirs))` with:

```python
                   <= tr.effective_range(bf, u, e) and bf.has_line_of_fire(u, e) for e in theirs))
```

In `_pick_focus_target`, replace:

```python
            for u in shooters:
                rng_u = max(u._max_range, max((sp.porte for sp in u.spells), default=0))
```

with:

```python
            for u in shooters:
                rng_u = max(tr.effective_range(bf, u, e),
                            max((sp.porte for sp in u.spells), default=0))
```

In `select_tactical_target`, replace in `_reachable`:

```python
        if d > max_range:
```

with:

```python
        if d > tr.effective_range(battlefield, unit, e):
```

- [ ] **Step 8: Run tests**

Run: `python test_terrain.py` → `Tous les tests terrain passent.`
Run: `python test_edge_cases.py` → `Tous les cas limites passent.`
Run: `python test_ai_headless.py` → ends with `Tous les tests terminés.`, no traceback.
Run: `python test_determinism.py` → `OK   3 exécutions identiques`.
Run: `python bench_balance.py 60 > ../docs/superpowers/baselines/bench-after-task5.txt` then compare with `bench-before-terrain.txt`: current maps carry no terrain yet, so win counts, mean rounds and survivors must be **identical** (only the timing line differs). Any difference means a neutral path is not neutral: fix before committing.

- [ ] **Step 9: Commit**

```bash
git add unit.py tactics.py battle.py battlefield.py ai_commander.py test_terrain.py ../docs/superpowers/baselines/bench-after-task5.txt
git commit -m "feat(terrain): couvert, hauteur et sauvegarde en combat; charge et portée effective

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: Outils de carte + Prairie enrichie

**Files:**
- Modify: `src/maps.py` (import; `_connected` terrain-aware; new `_mirror_terrain`, `_paint_disc`, `_bfs_path`; `generate_prairie`)
- Test: `src/test_terrain.py`

**Interfaces:**
- Produces (in `maps`):
  - `_connected(grid, width, height, start, goal, terrain=None, avoid=())` (existing callers unchanged)
  - `_mirror_terrain(terr, width, height) -> None`
  - `_paint_disc(terr, cx, cy, r, name, width, height) -> None` (float centre allowed)
  - `_bfs_path(grid, width, height, starts, goals, terrain=None, avoid=(), blocked=()) -> list[tuple] | None`

- [ ] **Step 1: Write the failing tests**

Add above the runner marker in `src/test_terrain.py`:

```python
# ── Cartes ──

import maps

SIZES = [(40, 30), (178, 64)]


def map_checks(name, seeds=10, sizes=SIZES, min_paths=2):
    for w, h in sizes:
        for seed in range(seeds):
            random.seed(500 + seed)
            grid, data = maps.generate_map(name, w, h)
            terr = data.get('terrain')
            assert terr is not None, f"{name}: pas de terrain"
            # Symétrie en miroir sur les cases libres des deux côtés
            for x in range(w // 2):
                for y in range(h):
                    xm = w - 1 - x
                    if grid[x][y] == 0 and grid[xm][y] == 0:
                        assert terr[x][y] == terr[xm][y], (name, w, h, seed, x, y, terr[x][y], terr[xm][y])
            left = [(1, y) for y in range(h) if grid[1][y] == 0 and tr.MOVE[terr[1][y]] is not None]
            right = [(w - 2, y) for y in range(h) if grid[w - 2][y] == 0 and tr.MOVE[terr[w - 2][y]] is not None]
            p1 = maps._bfs_path(grid, w, h, left, right, terr)
            assert p1, f"{name} {w}x{h} graine {seed}: aucun passage"
            if min_paths >= 2:
                blocked = set(p1[1:-1])
                p2 = maps._bfs_path(grid, w, h, left, right, terr, blocked=blocked)
                assert p2, f"{name} {w}x{h} graine {seed}: un seul passage"
            assert maps._bfs_path(grid, w, h, left, right, terr, avoid=(tr.MARSH,)), \
                f"{name} {w}x{h} graine {seed}: aucun passage sans marais"


def deploy_check(name, sizes=SIZES, seeds=4):
    import unit_library as ul
    from battle import Battle
    for w, h in sizes:
        for seed in range(seeds):
            random.seed(900 + seed)
            a1 = ul.build_army("Armée Skaldienne", [("Infanterie régulière", 12), ("Arbaletrier régulier", 6)])
            a2 = ul.build_army("Armée Orlandar", [("Fantassin covaliir", 12), ("Archer covaliir", 6), ("Cavalier covaliir", 3)])
            b = Battle(a1, a2, w, h, 8, map_name=name)
            bf = b.battlefield
            for u in b.army1 + b.army2:
                t = tr.at(bf, *u.position)
                assert tr.MOVE[t] == 1.0, f"{name} {w}x{h}: {u.name} déployé sur {t} en {u.position}"


@test
def test_connected_is_terrain_aware():
    grid = [[0] * 5 for _ in range(7)]
    terr = tr.make_grid(7, 5)
    for y in range(5):
        terr[3][y] = tr.RIVER
    assert maps._connected(grid, 7, 5, (0, 2), (6, 2))            # ancien appel
    assert not maps._connected(grid, 7, 5, (0, 2), (6, 2), terr)
    terr[3][4] = tr.MARSH
    assert maps._connected(grid, 7, 5, (0, 2), (6, 2), terr)
    assert not maps._connected(grid, 7, 5, (0, 2), (6, 2), terr, avoid=(tr.MARSH,))


@test
def test_map_prairie():
    map_checks("Prairie")
    deploy_check("Prairie")
    random.seed(1)
    grid, data = maps.generate_map("Prairie", 178, 64)
    names = {n for col in data['terrain'] for n in col}
    assert {tr.HILL, tr.WOOD} <= names, names
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `python test_terrain.py connected` → ECHEC (`TypeError: _connected() takes 5 positional arguments`).
Run: `python test_terrain.py map_prairie` → ECHEC (`pas de terrain`).

- [ ] **Step 3: Map helpers**

In `src/maps.py`, after `import random` add `import terrain as tr`.

Replace `_connected` with:

```python
def _connected(grid, width, height, start, goal, terrain=None, avoid=()):
    """Vrai si goal est atteignable depuis start (8-voisinage, cases libres).
    Avec `terrain`: la rivière bloque, et les terrains de `avoid` aussi."""
    return _bfs_path(grid, width, height, [start], [goal], terrain, avoid) is not None


def _bfs_path(grid, width, height, starts, goals, terrain=None, avoid=(), blocked=()):
    """Plus court chemin (en cases, 8-voisinage) d'une case de `starts` à une
    case de `goals`, ou None. `blocked`: cases interdites en plus."""
    goals = set(goals)
    blocked = set(blocked)

    def ok(x, y):
        if grid[x][y] != 0 or (x, y) in blocked:
            return False
        if terrain is not None:
            name = terrain[x][y]
            if tr.MOVE[name] is None or name in avoid:
                return False
        return True

    from collections import deque
    came = {}
    queue = deque()
    for s in starts:
        if ok(*s) and s not in came:
            came[s] = None
            queue.append(s)
    while queue:
        cur = queue.popleft()
        if cur in goals:
            path = []
            while cur is not None:
                path.append(cur)
                cur = came[cur]
            return path[::-1]
        cx, cy = cur
        for dx in (-1, 0, 1):
            for dy in (-1, 0, 1):
                nx, ny = cx + dx, cy + dy
                if (0 <= nx < width and 0 <= ny < height and (nx, ny) not in came
                        and ok(nx, ny)):
                    came[(nx, ny)] = cur
                    queue.append((nx, ny))
    return None


def _mirror_terrain(terr, width, height):
    """Recopie la moitié ouest sur l'est (x → width-1-x): aucun camp n'est
    avantagé par le terrain."""
    for x in range(width // 2):
        for y in range(height):
            terr[width - 1 - x][y] = terr[x][y]


def _paint_disc(terr, cx, cy, r, name, width, height):
    """Peint un disque de terrain (centre réel autorisé)."""
    ri = int(math.ceil(r)) + 1
    for x in range(int(cx) - ri, int(cx) + ri + 2):
        for y in range(int(cy) - ri, int(cy) + ri + 2):
            if 0 <= x < width and 0 <= y < height and (x - cx) ** 2 + (y - cy) ** 2 <= r * r:
                terr[x][y] = name
```

- [ ] **Step 4: Prairie terrain**

In `generate_prairie`, replace the final `return grid, {}` with:

```python
    # ── Terrain ──
    # Les crêtes deviennent des collines: on les tient au lieu d'y buter.
    # La moitié des rochers disparaît, le reste sert de couvert au sommet.
    for ry in ridge_ys:
        for x in range(ridge_start_x, ridge_end_x + 1):
            for y in range(ry - 2, ry + 3):
                if 0 <= x < width and 0 < y < height - 1 and grid[x][y] == 1 \
                        and random.random() < 0.5:
                    grid[x][y] = 0

    terr = tr.make_grid(width, height)
    half = width // 2
    for ry in ridge_ys:
        for x in range(ridge_start_x, half):
            for y in (ry - 1, ry, ry + 1):
                if 0 < y < height - 1:
                    terr[x][y] = tr.HILL
    # Broussailles sur les flancs, juste devant la ligne de déploiement:
    # de quoi masquer une cavalerie
    for by in (height / 8, 7 * height / 8):
        _paint_disc(terr, width * 0.44, by, 1.8, tr.WOOD, width, height)
    _mirror_terrain(terr, width, height)
    # Colline centrale basse: l'objectif naturel du couloir principal
    _paint_disc(terr, (width - 1) / 2, (height - 1) / 2, 3.0, tr.HILL, width, height)

    return grid, {'terrain': terr}
```

- [ ] **Step 5: Run tests**

Run: `python test_terrain.py connected` and `python test_terrain.py map_prairie` → OK.
If `deploy_check` fails on a wood cell: move the brush centre from `0.44` to `0.46` of the width and re-run (do not shrink below radius 1.5).
Run: `python test_edge_cases.py` → `Tous les cas limites passent.`

- [ ] **Step 6: Commit**

```bash
git add maps.py test_terrain.py
git commit -m "feat(cartes): outils miroir/chemins et Prairie à collines et broussailles

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: Forêt enrichie

**Files:**
- Modify: `src/maps.py` `generate_forest` (record trail ys; everything from the `# ── Champs de déploiement` comment to the `return`)
- Test: `src/test_terrain.py`

- [ ] **Step 1: Write the failing test**

Add above the runner marker:

```python
@test
def test_map_forest():
    map_checks("Forêt")
    deploy_check("Forêt")
    random.seed(2)
    grid, data = maps.generate_map("Forêt", 178, 64)
    names = {n for col in data['terrain'] for n in col}
    assert {tr.WOOD, tr.RIVER, tr.FORD} <= names, names
    assert data.get('deploy_gap'), "deploy_gap doit être conservé"
```

- [ ] **Step 2: Run to verify it fails**

Run: `python test_terrain.py map_forest` → ECHEC (`pas de terrain`).

- [ ] **Step 3: Record trail rows at the stream column**

In the trails block, before `for i in range(n_trails):` add `trail_ys = []`. Inside the inner `for x in range(x_start, x_end + 1):` loop, after the `_carve(...)` call, add:

```python
            if x == cx - 1:
                trail_ys.append(max(1, min(height - 2, y)))
```

- [ ] **Step 4: Forest terrain**

Replace everything from the comment `# ── Champs de déploiement dégagés: on se range hors du bois ──` to the end of the function with:

```python
    # ── Symétrie: l'ouest est recopié à l'est (terrain équitable) ──
    for x in range(width // 2):
        for y in range(height):
            grid[width - 1 - x][y] = grid[x][y]

    # ── Bosquets: le cœur reste impénétrable, le pourtour devient un
    # sous-bois traversable (lent, à couvert). On coupe à travers bois. ──
    terr = tr.make_grid(width, height)
    core = [[grid[x][y] == 1 and all(
                not (0 <= x + dx < width and 0 <= y + dy < height)
                or grid[x + dx][y + dy] == 1
                for dx in (-1, 0, 1) for dy in (-1, 0, 1))
             for y in range(height)] for x in range(width)]
    for x in range(width):
        for y in range(height):
            if grid[x][y] == 1 and not core[x][y]:
                grid[x][y] = 0
                terr[x][y] = tr.WOOD
    # Lisière clairsemée
    for x in range(width // 2):
        for y in range(1, height - 1):
            if (grid[x][y] == 0 and inside(x, y, 1.4) and not inside(x, y, 1.0)
                    and random.random() < 0.25):
                terr[x][y] = tr.WOOD
    _mirror_terrain(terr, width, height)

    # ── Ruisseau nord-sud au cœur du massif, franchissable à deux gués ──
    rcols = (cx - 1, cx)
    for y in range(1, height - 1):
        if inside(cx, y, 0.9):
            for x in rcols:
                grid[x][y] = 0
                terr[x][y] = tr.RIVER
    for ty in sorted(set(trail_ys), key=lambda t: abs(t - cy))[:2]:
        for y in (ty - 1, ty, ty + 1):
            if 0 < y < height - 1:
                for x in rcols:
                    if terr[x][y] == tr.RIVER:
                        terr[x][y] = tr.FORD

    # ── Champs de déploiement dégagés: on se range hors du bois ──
    deploy_gap = rx + 5
    for x in range(cx - deploy_gap - 5, cx - deploy_gap + 6):
        for xx in (x, width - 1 - x):
            if 0 <= xx < width:
                for y in range(height):
                    grid[xx][y] = 0
                    terr[xx][y] = tr.PLAIN

    # ── Garantie de passage d'un camp à l'autre ──
    left, right = (max(0, cx - int(rx * 2)), cy), (min(width - 1, cx + int(rx * 2)), cy)
    if not _connected(grid, width, height, left, right, terr):
        for x in range(left[0], right[0] + 1):
            _carve(grid, x, cy, 1.2, width, height)
            for y in (cy - 1, cy, cy + 1):
                if terr[x][y] == tr.RIVER:
                    terr[x][y] = tr.FORD

    return grid, {'deploy_gap': deploy_gap, 'terrain': terr}
```

- [ ] **Step 5: Run tests**

Run: `python test_terrain.py map_forest` → OK.
Run: `python test_edge_cases.py` → `Tous les cas limites passent.`
If `map_checks` reports "un seul passage" at 40x30: the massif is small (rx=5); lower the stream extent from `inside(cx, y, 0.9)` to `inside(cx, y, 0.75)` and re-run.

- [ ] **Step 6: Commit**

```bash
git add maps.py test_terrain.py
git commit -m "feat(cartes): Forêt à sous-bois traversable, ruisseau et gués

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 8: Village enrichi

**Files:**
- Modify: `src/maps.py` `generate_village` (final `return`)
- Test: `src/test_terrain.py`

- [ ] **Step 1: Write the failing test**

```python
@test
def test_map_village():
    map_checks("Village")
    deploy_check("Village")
    random.seed(4)
    grid, data = maps.generate_map("Village", 178, 64)
    names = {n for col in data['terrain'] for n in col}
    assert {tr.HILL, tr.WOOD, tr.MARSH} <= names, names
```

- [ ] **Step 2: Run to verify it fails**

Run: `python test_terrain.py map_village` → ECHEC (`pas de terrain`).

- [ ] **Step 3: Village terrain**

Replace `return grid, {'deploy_gap': R + 5}` with:

```python
    # ── Terrain ──
    terr = tr.make_grid(width, height)
    mx = (width - 1) / 2
    # Le bourg est sur une butte: ses rues dominent les champs
    _paint_disc(terr, mx, cy, R + 1.0, tr.HILL, width, height)
    # Jardins et vergers entre les maisons (jamais dans une rue, ni la
    # nôtre ni celle d'en face une fois le terrain mis en miroir)
    for x in range(width // 2):
        for y in range(1, height - 1):
            d = math.hypot(x - mx, y - cy)
            if not (R * 0.55 <= d <= R - 0.5) or grid[x][y] != 0:
                continue
            if in_street(x, y) or in_street(width - 1 - x, y):
                continue
            if random.random() < 0.30:
                terr[x][y] = tr.WOOD
    # Mare boueuse au bord de la place, hors de la grand-rue
    _paint_disc(terr, mx - (plaza + 2.5), cy + plaza + 1.5, 1.6, tr.MARSH, width, height)
    _mirror_terrain(terr, width, height)

    return grid, {'deploy_gap': R + 5, 'terrain': terr}
```

Note: `_mirror_terrain` copies the whole west half, so the butte (already symmetric about `mx`) is preserved and the mare appears on both sides of the plaza.

- [ ] **Step 4: Run tests**

Run: `python test_terrain.py map_village` → OK.
Run: `python test_edge_cases.py` → `Tous les cas limites passent.`

- [ ] **Step 5: Commit**

```bash
git add maps.py test_terrain.py
git commit -m "feat(cartes): Village sur butte, jardins et mare

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 9: Défilé enrichi

The throat of the existing Défilé can be a single row high at 40x30 (squeeze 5): it is a one-cell choke by design. The "2 disjoint passages" check therefore runs at 178x64 only for this map; 40x30 checks a single passage.

**Files:**
- Modify: `src/maps.py` `generate_defile` (final `return`)
- Test: `src/test_terrain.py`

- [ ] **Step 1: Write the failing test**

```python
@test
def test_map_defile():
    map_checks("Défilé", sizes=[(178, 64)])
    map_checks("Défilé", sizes=[(40, 30)], min_paths=1)
    deploy_check("Défilé")
    random.seed(5)
    grid, data = maps.generate_map("Défilé", 178, 64)
    names = {n for col in data['terrain'] for n in col}
    assert {tr.HILL, tr.MARSH, tr.RIVER, tr.BRIDGE, tr.FORD} <= names, names
```

- [ ] **Step 2: Run to verify it fails**

Run: `python test_terrain.py map_defile` → ECHEC (`pas de terrain`).

- [ ] **Step 3: Défilé terrain**

Replace the final `return grid, {}` with:

```python
    # ── Terrain ──
    terr = tr.make_grid(width, height)
    cx = width // 2
    for x in range(width):
        in_throat = throat_start <= x < throat_end
        top_b = pass_top + (throat_squeeze if in_throat else 0)
        bot_b = pass_bot - (throat_squeeze if in_throat else 0)
        # Pentes au pied des parois: les tireurs y dominent le couloir
        for y in (top_b, top_b + 1, bot_b - 1, bot_b):
            if 0 <= y < height:
                terr[x][y] = tr.HILL
        # Éboulis boueux près du torrent, seulement si le couloir est
        # assez large pour laisser un passage sec au milieu
        if bot_b - top_b >= 6:
            if cx - 6 <= x <= cx - 3:
                terr[x][top_b + 2] = tr.MARSH
            if cx - 9 <= x <= cx - 6:
                terr[x][bot_b - 2] = tr.MARSH
    _mirror_terrain(terr, width, height)

    # Torrent à l'étranglement: un pont et un gué à disputer
    t_top = pass_top + throat_squeeze
    t_bot = pass_bot - throat_squeeze
    rcols = (cx - 1, cx)
    free = list(range(t_top, t_bot + 1))
    for x in rcols:
        for y in free:
            grid[x][y] = 0          # les saillies cèdent la place au torrent
            terr[x][y] = tr.RIVER
    if len(free) >= 4:
        mid = len(free) // 2
        bridge = free[mid - 2:mid]
        ford = free[mid + 1:mid + 3]
    else:
        bridge, ford = free, []
    for x in rcols:
        for y in bridge:
            terr[x][y] = tr.BRIDGE
        for y in ford:
            terr[x][y] = tr.FORD

    return grid, {'terrain': terr}
```

- [ ] **Step 4: Run tests**

Run: `python test_terrain.py map_defile` → OK.
The small-map assertion `{…FORD}` is only made at 178x64; that is intended.
Run: `python test_edge_cases.py` → `Tous les cas limites passent.`

- [ ] **Step 5: Commit**

```bash
git add maps.py test_terrain.py
git commit -m "feat(cartes): Défilé à pentes, éboulis, torrent, pont et gué

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 10: Décor aligné sur le terrain

**Files:**
- Modify: `src/maps.py` (`generate_decor` signature and loop; `generate_map` call)
- Test: `src/test_terrain.py`

- [ ] **Step 1: Write the failing test**

```python
@test
def test_decor_follows_terrain():
    random.seed(6)
    grid, data = maps.generate_map("Forêt", 178, 64)
    terr = data['terrain']
    wet = {tr.RIVER, tr.FORD, tr.BRIDGE}
    trees = {"arbre_pin", "arbre_rond"}
    on_wood = off_wood = 0
    for x, y, kind, _ in data['decor']:
        assert terr[x][y] not in wet, (x, y, kind, terr[x][y])
        if kind in trees:
            if terr[x][y] == tr.WOOD:
                on_wood += 1
            else:
                off_wood += 1
    assert on_wood > off_wood, (on_wood, off_wood)
```

- [ ] **Step 2: Run to verify it fails**

Run: `python test_terrain.py decor` → ECHEC (props on river cells, or trees mostly outside woods).

- [ ] **Step 3: Implement**

Add near `_DECOR_TABLES`:

```python
# Sous-bois: c'est là, et plus au hasard, que poussent les arbres
_WOOD_DECOR = {
    'density': 0.45,
    'big': [("arbre_pin", 3), ("arbre_rond", 4), ("buisson", 3)],
}
_NO_DECOR_TERRAIN = {tr.RIVER, tr.FORD, tr.BRIDGE}
```

Change the signature to `def generate_decor(map_name, grid, width, height, terrain=None):` and replace the loop head:

```python
    for x in range(1, width - 1):
        for y in range(1, height - 1):
            if grid[x][y] != 0:
                continue
            if rng.random() > density:
                continue
```

with:

```python
    for x in range(1, width - 1):
        for y in range(1, height - 1):
            if grid[x][y] != 0:
                continue
            tname = terrain[x][y] if terrain is not None else tr.PLAIN
            if tname in _NO_DECOR_TERRAIN:
                continue
            if tname == tr.WOOD:
                if rng.random() > _WOOD_DECOR['density']:
                    continue
                if any((x + dx, y + dy) in occupied
                       for dx in (-1, 0, 1) for dy in (-1, 0, 1)):
                    continue
                occupied.add((x, y))
                props.append((x, y, _weighted_pick(rng, _WOOD_DECOR['big']),
                              rng.randrange(1 << 16)))
                continue
            if rng.random() > density:
                continue
```

For non-wood cells, forbid trees so that trees read as "wood": right after `kind = _weighted_pick(rng, table['big'])`, add:

```python
                if terrain is not None and kind in ("arbre_pin", "arbre_rond"):
                    kind = "buisson"
```

In `generate_map`, replace:

```python
    map_data['decor'] = generate_decor(map_name, grid, width, height)
```

with:

```python
    map_data['decor'] = generate_decor(map_name, grid, width, height,
                                       map_data.get('terrain'))
```

- [ ] **Step 4: Run tests**

Run: `python test_terrain.py` → `Tous les tests terrain passent.`

- [ ] **Step 5: Commit**

```bash
git add maps.py test_terrain.py
git commit -m "feat(cartes): décor aligné sur le terrain (arbres dans les bois, rien sur l'eau)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 11: Rendu du terrain et légende (touche L)

**Files:**
- Create: `src/terrain_render.py`
- Modify: `src/renderer.py` (`build_grid_surface`, `run_visual` key handling, overlay, help line)
- Test: `src/test_terrain.py`

**Interfaces:**
- Produces: `terrain_render.draw_terrain(surf, bf, cs) -> None`; `terrain_render.legend_surface(bf, font) -> pygame.Surface | None` (None when `bf.terrain is None`); `terrain_render.LEGEND` (dict name → text).

- [ ] **Step 1: Write the failing test**

```python
@test
def test_render_terrain_smoke():
    os.environ.setdefault("SDL_VIDEODRIVER", "dummy")
    import pygame
    pygame.init()
    pygame.display.set_mode((1, 1))
    import terrain_render as trr
    from renderer import build_grid_surface
    from battle import Battle
    import unit_library as ul
    random.seed(8)
    for name in ("Prairie", "Forêt", "Village", "Défilé", "Siège"):
        a = ul.build_army("Armée Skaldienne", [("Infanterie régulière", 3)])
        b = Battle(a, a, 60, 40, 8, map_name=name)
        surf = build_grid_surface(b, 16)
        assert surf.get_width() == 60 * 16
        leg = trr.legend_surface(b.battlefield, pygame.font.SysFont("arial", 14))
        if name == "Siège":
            assert leg is None
        else:
            assert leg is not None and leg.get_height() > 20
    # Une case de rivière est dessinée en bleu
    random.seed(5)
    b = Battle(a, a, 178, 64, 8, map_name="Défilé")
    bf = b.battlefield
    surf = build_grid_surface(b, 16)
    rx, ry = next((x, y) for x in range(bf.width) for y in range(bf.height)
                  if bf.terrain[x][y] == tr.RIVER and bf.grid[x][y] == 0)
    r, g, bl, _ = surf.get_at((rx * 16 + 8, ry * 16 + 8))
    assert bl > r and bl > g - 10, (r, g, bl)
    assert set(trr.LEGEND) >= {tr.HILL, tr.WOOD, tr.RIVER, tr.FORD, tr.BRIDGE, tr.MARSH}
```

- [ ] **Step 2: Run to verify it fails**

Run: `python test_terrain.py render` → ECHEC (`No module named 'terrain_render'`).

- [ ] **Step 3: Write `src/terrain_render.py`**

```python
"""Rendu du terrain à effets.

Règle de lisibilité: chaque terrain a une teinte ET un motif (courbes,
hachures, ondes, planches…). On ne doit jamais avoir besoin de distinguer
deux couleurs proches pour savoir où l'on marche.
"""
import pygame

import terrain as tr

LEGEND = {
    tr.HILL:   "Colline — tir: +1 portée vers le bas · mêlée: -1 pour toucher qui est en haut · monter ×1,5",
    tr.WOOD:   "Bois — déplacement ×2 · tirs reçus: -1 pour toucher · pas de charge · 3 cases masquent la vue",
    tr.RIVER:  "Rivière — infranchissable",
    tr.FORD:   "Gué — déplacement ×2 · sauvegarde -1 · pas de charge",
    tr.BRIDGE: "Pont — passage étroit, déplacement normal",
    tr.MARSH:  "Marais — déplacement ×3 · sauvegarde -1 · pas de charge",
}
_ORDER = (tr.HILL, tr.WOOD, tr.RIVER, tr.FORD, tr.BRIDGE, tr.MARSH)


def _seed(x, y):
    return ((x * 73856093) ^ (y * 19349663)) & 0xFFFF


def _is(bf, x, y, name):
    return 0 <= x < bf.width and 0 <= y < bf.height and bf.terrain[x][y] == name


def draw_cell(ov, name, r, cs, sd=0, edges=(False, False, False, False)):
    """Dessine une case de terrain sur une surface SRCALPHA.
    edges = (haut, droite, bas, gauche): bord de la zone (pour les contours)."""
    x0, y0 = r.x, r.y
    lw = max(1, cs // 12)
    if name == tr.HILL:
        ov.fill((255, 238, 190, 34), r)
        if (sd & 1) == 0 and cs >= 10:
            mx, my = x0 + cs // 2, y0 + cs // 2 + cs // 8
            w = cs // 4
            pygame.draw.lines(ov, (255, 246, 214, 90), False,
                              [(mx - w, my), (mx, my - w), (mx + w, my)], lw)
        dark = (52, 38, 14, 150)
        top, right, bottom, left = edges
        if top:
            pygame.draw.line(ov, dark, (x0, y0), (x0 + cs - 1, y0), lw)
        if right:
            pygame.draw.line(ov, dark, (x0 + cs - 1, y0), (x0 + cs - 1, y0 + cs - 1), lw)
        if bottom:
            pygame.draw.line(ov, dark, (x0, y0 + cs - 1), (x0 + cs - 1, y0 + cs - 1), lw)
        if left:
            pygame.draw.line(ov, dark, (x0, y0), (x0, y0 + cs - 1), lw)
    elif name == tr.WOOD:
        ov.fill((8, 28, 6, 80), r)
        for k in range(3):
            px = x0 + 2 + ((sd >> (k * 3)) % max(1, cs - 4))
            py = y0 + 2 + ((sd >> (k * 3 + 5)) % max(1, cs - 4))
            pygame.draw.circle(ov, (16, 56, 18, 190), (px, py), max(1, cs // 7))
    elif name == tr.RIVER:
        ov.fill((44, 92, 138, 245), r)
        for k in (1, 3):
            yy = y0 + k * cs // 4 + ((sd >> k) & 1)
            pts = [(x0 + i * cs // 4, yy + (1 if i % 2 else -1)) for i in range(5)]
            pygame.draw.lines(ov, (150, 200, 235, 150), False, pts, 1)
    elif name == tr.FORD:
        ov.fill((88, 132, 152, 215), r)
        for k in range(3):
            px = x0 + 2 + ((sd >> (k * 4)) % max(1, cs - 4))
            py = y0 + 2 + ((sd >> (k * 4 + 2)) % max(1, cs - 4))
            pygame.draw.circle(ov, (176, 170, 150, 230), (px, py), max(1, cs // 9))
    elif name == tr.BRIDGE:
        ov.fill((118, 86, 52, 255), r)
        step = max(3, cs // 4)
        for xx in range(x0 + step, x0 + cs, step):
            pygame.draw.line(ov, (70, 50, 30, 255), (xx, y0), (xx, y0 + cs - 1), 1)
        pygame.draw.line(ov, (58, 40, 22, 255), (x0, y0), (x0 + cs - 1, y0), lw)
        pygame.draw.line(ov, (58, 40, 22, 255), (x0, y0 + cs - 1), (x0 + cs - 1, y0 + cs - 1), lw)
    elif name == tr.MARSH:
        ov.fill((58, 70, 38, 130), r)
        step = max(3, cs // 3)
        for k in range(-cs, cs, step):
            pygame.draw.line(ov, (28, 40, 20, 170), (x0 + k, y0 + cs - 1), (x0 + k + cs - 1, y0), 1)
        if cs >= 10:
            tx, ty = x0 + cs // 3 + (sd & 3), y0 + cs // 2
            pygame.draw.line(ov, (130, 150, 76, 210), (tx, ty), (tx, ty - cs // 4), 1)
            pygame.draw.line(ov, (130, 150, 76, 210), (tx + 2, ty), (tx + 3, ty - cs // 5), 1)


def draw_terrain(surf, bf, cs):
    """Pose les motifs de terrain sur la surface statique de la grille."""
    if getattr(bf, 'terrain', None) is None:
        return
    ov = pygame.Surface(surf.get_size(), pygame.SRCALPHA)
    for x in range(bf.width):
        col = bf.terrain[x]
        for y in range(bf.height):
            name = col[y]
            if name == tr.PLAIN or bf.grid[x][y] != 0:
                continue
            edges = (not _is(bf, x, y - 1, name), not _is(bf, x + 1, y, name),
                     not _is(bf, x, y + 1, name), not _is(bf, x - 1, y, name))
            draw_cell(ov, name, pygame.Rect(x * cs, y * cs, cs, cs), cs, _seed(x, y), edges)
    surf.blit(ov, (0, 0))


def legend_surface(bf, font):
    """Encart des terrains présents sur la carte (None s'il n'y en a pas)."""
    if getattr(bf, 'terrain', None) is None:
        return None
    present = {n for col in bf.terrain for n in col}
    rows = [n for n in _ORDER if n in present]
    if not rows:
        return None
    sw, pad, gap = 22, 10, 6
    texts = [font.render(LEGEND[n], True, (228, 228, 220)) for n in rows]
    title = font.render("Terrain  (L pour masquer)", True, (255, 220, 120))
    w = pad * 2 + max([sw + 10 + t.get_width() for t in texts] + [title.get_width()])
    h = pad * 2 + title.get_height() + gap + sum(max(sw, t.get_height()) + gap for t in texts)
    surf = pygame.Surface((w, h), pygame.SRCALPHA)
    surf.fill((12, 14, 18, 215))
    pygame.draw.rect(surf, (255, 220, 120, 150), surf.get_rect(), 1)
    surf.blit(title, (pad, pad))
    y = pad + title.get_height() + gap
    for n, t in zip(rows, texts):
        box = pygame.Rect(pad, y, sw, sw)
        pygame.draw.rect(surf, (72, 88, 56, 255), box)
        draw_cell(surf, n, box, sw, 0x5A5A, (True, True, True, True))
        pygame.draw.rect(surf, (200, 200, 200, 200), box, 1)
        surf.blit(t, (pad + sw + 10, y + (sw - t.get_height()) // 2))
        y += max(sw, t.get_height()) + gap
    return surf
```

- [ ] **Step 4: Hook into renderer.py**

In `build_grid_surface`, right after the ground patches block:

```python
    if patches and cell_size >= 12:
        draw_ground_patches(grid_surface, patches, cell_size, bg)
```

add:

```python
    # ─── Terrain à effets: teinte + motif (colline, bois, eau, marais) ───
    import terrain_render
    terrain_render.draw_terrain(grid_surface, bf, cell_size)
```

In `run_visual`, next to `show_lines = True` (line ~1111), add:

```python
    show_terrain_legend = False
    terrain_legend = None
```

In the KEYDOWN chain, after the `K_t` branch, add:

```python
                elif event.key == pygame.K_l:
                    show_terrain_legend = not show_terrain_legend
                    terrain_legend = None   # reconstruit au prochain affichage
```

In the `K_r` (restart) branch, add `terrain_legend = None` next to `battle_report = None`.

Right before the `# ═══ OVERLAY PAUSE ═══` comment, add:

```python
        # ═══ LÉGENDE DU TERRAIN (touche L) ═══
        if show_terrain_legend:
            if terrain_legend is None:
                import terrain_render
                terrain_legend = terrain_render.legend_surface(battle.battlefield, small_font)
            if terrain_legend is not None:
                screen.blit(terrain_legend, (12, top_h + 12))
```

(`top_h` is defined at `renderer.py:1638` in the same frame loop, height of the top bar.)

Replace the help line:

```python
        ctrl = tiny_font.render("ESPACE=Pause  ZQSD/Flèches=Caméra  F=Vite  N=Normal  R=Reset  T=Lignes  B=Bordure  M=Menu  ESC=Quit", True, (150, 170, 200))
```

with:

```python
        ctrl = tiny_font.render("ESPACE=Pause  ZQSD/Flèches=Caméra  F=Vite  N=Normal  R=Reset  T=Lignes  L=Terrain  B=Bordure  M=Menu  ESC=Quit", True, (150, 170, 200))
```

- [ ] **Step 5: Run tests**

Run: `python test_terrain.py render` → OK.
Run: `python test_terrain.py` → `Tous les tests terrain passent.`

- [ ] **Step 6: Visual check in the real game**

Use the `run` skill (or `python main.py` from `src`) and for each of Prairie, Forêt, Village, Défilé: launch a battle, press `L`, take a screenshot. Check: hills show contour lines, woods show dark canopy dots, water is blue with waves, bridge has planks, marsh has hatching; the legend lists only terrains present; `Siège` shows no legend. Report the screenshots to the user.

- [ ] **Step 7: Commit**

```bash
git add terrain_render.py renderer.py test_terrain.py
git commit -m "feat(rendu): motifs de terrain et légende (touche L)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 12: Validation globale, équilibre, rythme, README

**Files:**
- Create: `docs/superpowers/baselines/bench-after-terrain.txt`
- Create: `src/measure_contact.py`
- Modify: `src/test_edge_cases.py` (none needed unless a case fails), `README.md`

- [ ] **Step 1: Full suites**

Run from `src`:
- `python test_terrain.py` → `Tous les tests terrain passent.`
- `python test_edge_cases.py` → `Tous les cas limites passent.`
- `python test_ai_headless.py` → `Tous les tests terminés.`, no traceback
- `python test_determinism.py` → `OK   3 exécutions identiques`

- [ ] **Step 2: Balance**

Run: `python bench_balance.py 60 > ../docs/superpowers/baselines/bench-after-terrain.txt`
Compare with `bench-before-terrain.txt` line by line. Win rate of army 1 = `A1/60`. Criterion: every matchup within **10 points** (i.e. `|A1_after − A1_before| ≤ 6`). Siège must be **identical** (no terrain on that map). Timing line ≤ **1.2 ×** before.

If a matchup drifts more than 10 points, use superpowers:systematic-debugging: rerun that matchup with `python bench_balance.py 60 2000` to rule out seed noise, then reduce the responsible terrain (smaller discs, fewer marsh cells) in its generator. Do not change combat numbers from the spec without asking the user.

- [ ] **Step 3: First contact round**

Create `src/measure_contact.py`:

```python
"""Round du premier corps-à-corps, par carte (taille réelle 178x64)."""
import os, sys, random
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import unit_library as ul
from battle import Battle

N = int(sys.argv[1]) if len(sys.argv) > 1 else 6
for m in ("Prairie", "Forêt", "Village", "Défilé"):
    rounds = []
    for seed in range(N):
        random.seed(300 + seed)
        a1 = ul.build_army("Armée Skaldienne", [("Infanterie régulière", 10), ("Arbaletrier régulier", 4)])
        a2 = ul.build_army("Armée Orlandar", [("Fantassin covaliir", 10), ("Archer covaliir", 4)])
        b = Battle(a1, a2, 178, 64, 8, map_name=m)
        contact = None
        while not b.is_battle_over() and b.round <= 40 and contact is None:
            b.simulate_round()
            for u in b.army1:
                if u.is_alive and any(e.is_alive and abs(u.position[0] - e.position[0])
                                      + abs(u.position[1] - e.position[1]) <= 1 for e in b.army2):
                    contact = b.round - 1
                    break
        rounds.append(contact if contact is not None else 99)
    print(f"{m:10s} premier contact moyen: round {sum(rounds) / len(rounds):.1f}  {rounds}")
```

Run it on `main` (before terrain) with `git stash`-free method: `git worktree add ../wo-before main` then `python ../wo-before/src/measure_contact.py` — copy `measure_contact.py` there first. Then run `python measure_contact.py` on this branch.
Criterion (spec): Prairie average contact round must not move later by more than **2 rounds**. If it does, shrink `_paint_disc` radii for wood in `generate_prairie`. Remove the worktree afterwards: `git worktree remove ../wo-before`.

- [ ] **Step 4: README**

In `README.md`:
1. In "Contrôles en bataille", add a row `| \`L\` | Afficher/masquer la légende du terrain |`.
2. After the "Cartes disponibles" table, add:

```markdown
### Terrain

Chaque case porte un terrain qui change la manière de se battre. Chaque
terrain a une teinte **et** un motif, pour rester lisible sans distinguer
les couleurs. `L` affiche la légende en bataille.

| Terrain | Déplacement | Vue | Combat |
|---------|-------------|-----|--------|
| **Colline** | ×1,5 pour monter | un tireur en hauteur voit par-dessus les bois; une colline masque ce qui est derrière | tireur en hauteur: +1 portée; frapper vers le haut: -1 pour toucher |
| **Bois** | ×2 | 3 cases de bois masquent la cible | tirs reçus: -1 pour toucher; pas de charge |
| **Rivière** | infranchissable | — | — |
| **Gué** | ×2 | — | sauvegarde -1; pas de charge |
| **Pont** | normal | — | passage étroit |
| **Marais** | ×3 | — | sauvegarde -1; pas de charge |

Une unité fait toujours au moins un pas par round, même en marais.
```

3. Update the map table descriptions: Prairie "crêtes en collines, colline centrale, broussailles sur les flancs"; Forêt "bosquets à cœur impénétrable et sous-bois traversable, ruisseau à deux gués"; Village "bourg sur une butte, jardins, mare"; Défilé "pentes, éboulis, torrent avec pont et gué".
4. In "Tests", replace the line citing `tests/test_main.py` with:

```bash
python src/test_terrain.py                  # terrain: règles, cartes, rendu
python src/test_determinism.py              # une graine rejoue la même bataille
```

5. In "Architecture du projet", add `terrain.py  # Règles de terrain (coûts, vue, modificateurs)` and `terrain_render.py  # Motifs et légende du terrain`.

- [ ] **Step 5: Commit**

```bash
git add ../README.md measure_contact.py ../docs/superpowers/baselines/bench-after-terrain.txt
git commit -m "docs: terrain à effets, cartes enrichies; mesures d'équilibre et de rythme

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

- [ ] **Step 6: Report**

Report to the user: the before/after bench table, the contact rounds, the screenshots from Task 11, and any criterion that was not met (with numbers). Then use superpowers:finishing-a-development-branch.
